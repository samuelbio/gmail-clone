import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gmailclone.wedevnow.com',
  appName: 'Gmail clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
